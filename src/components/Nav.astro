---
// Nav — built in Step 2
interface Props {
  lang: 'es' | 'en';
  currentPath: string;
}
const { lang, currentPath } = Astro.props;
---

<!-- TODO Step 2: full Nav with language toggle, mobile hamburger, CTA -->
<nav><!-- placeholder --></nav>
